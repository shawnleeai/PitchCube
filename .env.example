# ============================================================================
# PitchCube - 环境变量配置模板
# ============================================================================
# 重要安全提示：
# 1. 复制此文件为 .env 并填入你的实际配置
# 2. NEVER 将 .env 文件提交到 Git!
# 3. 生产环境建议使用密钥管理服务（如 AWS Secrets Manager、Azure Key Vault）
# ============================================================================

# =============================================================================
# 基础配置 (必需)
# =============================================================================

# 应用环境: development | production
NODE_ENV=development

# 调试模式: true | false
DEBUG=true

# =============================================================================
# 数据库配置 (本地开发必需)
# =============================================================================

# MongoDB 配置
MONGODB_URI=mongodb://localhost:27017/pitchcube
# 或带认证的连接字符串
# MONGODB_URI=mongodb://username:password@localhost:27017/pitchcube?authSource=admin

# MongoDB 密码 (如果使用 Docker Compose)
MONGODB_PASSWORD=change_this_to_secure_password

# Redis 配置
REDIS_URI=redis://localhost:6379/0
REDIS_PASSWORD=change_this_to_secure_password

# =============================================================================
# 安全配置 (生产环境必需)
# =============================================================================

# JWT 密钥 - 使用随机生成的强密码 (openssl rand -base64 32)
JWT_SECRET=change_this_to_a_random_32_char_string

# 加密密钥 - 用于敏感数据加密
ENCRYPTION_KEY=change_this_to_a_32_byte_key

# =============================================================================
# AI 服务 API 密钥 (可选，用于增强功能)
# =============================================================================
# 
# 获取方式：
# - OpenAI: https://platform.openai.com/api-keys
# - Stability AI: https://platform.stability.ai/
# 
# 安全提示：
# - 所有 API 调用都通过后端代理，前端不会直接暴露密钥
# - 建议为不同环境创建不同的 API 密钥
# - 启用 API 密钥的使用限额和监控

# OpenAI API (用于智能文案生成、视频脚本等)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Stability AI API (用于图像生成)
STABILITY_API_KEY=sk-your-stability-api-key-here

# =============================================================================
# 第三方服务集成 (可选)
# =============================================================================

# Azure Speech Services (用于语音合成功能)
# 获取方式: https://portal.azure.com/ -> Speech Services
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=westus

# 阶跃星辰 StepFun (国产 AI 服务，用于语音合成、图像生成等)
# 获取方式: https://platform.stepfun.com/
STEPFUN_API_KEY=your-stepfun-api-key-here
STEPFUN_TTS_MODEL=step-tts-mini  # 选项: step-tts-mini, step-tts-2

# 3D 打印服务 (可选)
CRAFTCLOUD_API_KEY=your-craftcloud-api-key
SHAPEWAYS_API_KEY=your-shapeways-api-key

# =============================================================================
# 云存储配置 (生产环境推荐)
# =============================================================================

# 阿里云 OSS
ALIBABA_OSS_ACCESS_KEY=your-access-key
ALIBABA_OSS_SECRET_KEY=your-secret-key
ALIBABA_OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
ALIBABA_OSS_BUCKET=pitchcube-assets

# AWS S3 (备选方案)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-west-2
AWS_S3_BUCKET=pitchcube-assets

# =============================================================================
# 前端配置
# =============================================================================

# API 基础 URL (前端调用)
NEXT_PUBLIC_API_URL=http://localhost:8000

# 应用公开 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# 高级配置 (可选)
# =============================================================================

# API 速率限制 (请求/分钟)
RATE_LIMIT_PER_MINUTE=60

# 文件上传限制 (MB)
MAX_UPLOAD_SIZE_MB=10

# 生成文件过期时间 (天)
GENERATED_FILES_EXPIRY_DAYS=7

# 日志级别: debug | info | warning | error
LOG_LEVEL=info
